{
  "name": "listcrunch",
  "version": "0.1.0",
  "description": "A JS port of MuckRock's listcrunch",
  "license": "MIT",
  "repository": "git@github.com:firstlookmedia/listcrunch.git",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build":  "tsc --build tsconfig.build.json",
    "build:watch": "yarn build --watch",
    "prepublishOnly": "yarn test && yarn build",
    "test": "jest",
    "test:watch": "yarn test --watch"
  },
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "@types/node": "^14.14.22",
    "jest": "^26.6.3",
    "jest-junit": "^12.0.0",
    "ts-jest": "^26.4.4",
    "typescript": "^4.1.3"
  }
}
